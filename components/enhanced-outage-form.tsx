"use client"

import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

/**
 * TEMPORARY stub so the app compiles.
 * Replace with the full enhanced outage form implementation once ready.
 */
export default function EnhancedOutageForm() {
  return (
    <Card className="w-full">
      <CardHeader>
        <CardTitle>Enhanced Outage Form (coming soon)</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">
          The full scheduling form is being rebuilt. Please use the dashboard while we finish this feature.
        </p>
      </CardContent>
    </Card>
  )
}
